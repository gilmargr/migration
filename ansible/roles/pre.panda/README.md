Pre Migration
=============

Create ssh id_rsa and API pem key.

Requirements
------------

- Linux shell (works on Windows WSL)
- Openssl installed

Example Playbook
----------------

> ansible-playbook pre_panda.yml
